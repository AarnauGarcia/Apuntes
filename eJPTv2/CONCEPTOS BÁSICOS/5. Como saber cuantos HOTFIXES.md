Para enumerarlos tendremos que iniciar una sesión de powershell y ejecutar este comando:

````
Get-HotFix
````

Si queremos contarlos automáticamente sería:

````
(Get-HotFix).Count
`````

Alternativa:

````
wmic qfe list brief /format:table
````

